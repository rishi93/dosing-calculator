version: '3'
services:
  web:
    build: .
    ports:
      - 8080:8080
    volumes:
      - .:/app
  db:
    image: mongo
    restart: always
    ports:
      - 27017:27017
    volumes:
      - mongodb:/data/db
volumes:
  mongodb:
